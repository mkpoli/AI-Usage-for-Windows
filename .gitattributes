# The golden fixture is compared byte for byte against the server output via
# include_str!, so it keeps LF endings whatever core.autocrlf a checkout uses.
src-tauri/src/local_http_api/rainmeter-sample.txt text eol=lf
